* Future [v1.0.0]:
	* TEST all the currently untested functions (including the ones from previous releases)
	* To test:
		26. object.descriptor:
			1. `Value()`-descriptor [with property behaviour, obviously]
			2. `Enumerable()`
			3. `Writable()`
			4. `Configurable()`
			5. `Value()` + `Configurable()` + `Writable()` + `Enumerable()`
			6. `Configurable()` + `Writable()`
			7. `Get()`
			8. `Set()`
			9. `Get()` + `Set()`
			10. `Get()` + `Set()` + `Configurable()`

	* TODO: fix the publish pipeline
		1. replace the expired NPM_TOKEN secret
		2. update version to 1.0.0 right before publishing
			[already have the workflow]

	* Update wiki [all the links - new AND old]

	Fix JSDOC:
		1. simplify, improve discriptiveness
		2. modules left:
			* inplace
			* object
			* types
			* functional
