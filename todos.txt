* Future [v1.0.0]:
	* TEST all the currently untested functions (including the ones from previous releases)
	* To test:
		21. object.classes.attachConst
		22. object.classes.attachGetter
		26. object.descriptor [everything]
		25. types.verifyAbstractConstructor [static test]

	* TODO: fix the publish pipeline
		1. replace the expired NPM_TOKEN secret
		2. update version to 1.0.0 right before publishing [already have the workflow]

	* Update wiki [all the links - new AND old]

	Fix JSDOC:
		1. simplify, improve discriptiveness
		2. modules left:
			* inplace
			* object
			* types
			* functional
