* Future [v1.0.0]:

	[note: the version is major since it is breaking,
		which is since:

		1. it forbids certain previously possible dubious mutating behaviours on a type-level
		2. rename `type` => `types` [breaking imports]
		3. enforcing the call to `types.verifyConstructor` to work with non-class constructors
			(cleaner and more concrete types then)
	]

	* To test:
		1. string.id.camelCase
		2. string.id.PascalCase
		3. string.id.kebabcase
		4. string.id.snake_case
		5. string.id.MACRO_CASE
		6. string.id.flatcase
		7. string.id.TRAIN_CASE
		8. string.id.HttpCase
		9. boolean.geq
		10. boolean.ge
		11. boolean.le
		12. boolean.leq
		13. number.isPositive
		14. number.isNegative
		15. number.isZero
		16. map.keys
		17. map.values
		18. array.fill
		19. array.from
		20. array.repeat
		21. object.descriptor.GetterDescriptor
		22. object.classes.attachConst
		23. object.classes.attachGetter
		24. types.verifyConstructor
		25. array.zeros

	* To JSDoc:
		1. string.id.camelCase
		2. string.id.PascalCase
		3. string.id.kebabcase
		4. string.id.snake_case
		5. string.id.MACRO_CASE
		6. string.id.flatcase
		7. string.id.TRAIN_CASE
		8. string.id.HttpCase
		9. boolean.geq
		10. boolean.ge
		11. boolean.le
		12. boolean.leq
		13. number.isPositive
		14. number.isNegative
		15. number.isZero
		16. map.keys
		17. map.values
		18. array.fill
		19. array.from
		20. array.repeat
		21. object.descriptor.GetterDescriptor
		22. object.classes.attachConst
		23. object.classes.attachGetter
		24. types.verifyConstructor
		25. array.zeros

	* TODO: fix the publish pipeline (replace the expired NPM_TOKEN secret, update version to 1.0.0 right before publishing)

	Add:

		3. types
			2. add 'AbstractConstructor<Signature extends any[] = [], Type = any> = abstract new (...s: Signature) => Type'
				2.1. likewise, add `types.verifyAbstractConstructor` (analogous to `types.verifyConstructor`)

	Fix:
			2. functional:
				* copy - add generics 'FunctionType extends Function':
					1. Do so for EVERY function in the library that accepts function-like objects,
						and which keeps the function's Shape;

			4. JSDOC:
				Fix bad jsdoc:
					1. fix the copypaste (example: 'object.empty' jsdoc is copypaste of 'array.empty');
					2. make it more descriptive
					3. add missing (listed above)
