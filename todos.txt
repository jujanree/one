* Future [v1.0.0]:
	* to jsdoc:
		1. object.recursiveIterate

	* TEST all the remaining currently untested functions from prev. releases:
		* array:
			1. insertion
			2. lastIndex
			3. isEmpty
		* inplace:
			1. firstOut
		* number:
			1. makeOdd
			2. makeEven
			3. difference
		* string:
			1. lastIndex
			2. charCodeAt
		* types:
			1. isStruct
		* object:
			1. recursiveIterate
		* functional:
			1. nil
			2. negate

	* To test [new]:
		26. object.descriptor:
			2. `Enumerable()`:
				1. one creates two descriptors - with and without Enumerable(); both use 'Value()'
				2. check that:
					1. enumerable is recognized by `targetObject.propertyIsEnumerable` and the other isn't
					2. enumerable is shown by `targetObject.property`
			3. `Writable()`
			4. `Configurable()`
				1. two descriptors - one with 'Writable()', another with 'Configurable()'
			7. `Get()`
			8. `Set()`
			9. `Get()` + `Set()`
			10. `Get()` + `Set()` + `Configurable()`

	* TODO: fix the publish pipeline
		1. replace the expired NPM_TOKEN secret
		2. update version to 1.0.0 right before publishing
			[already have the workflow]
		3. fix the publish variables (they could be outdated)

	* Update wiki [all the links - new AND old]

	Fix JSDOC:
		1. simplify, improve discriptiveness
		2. modules left:
			* inplace
			* object
			* types
			* functional
