* Future [v1.0.0]:
	* to jsdoc:
		1. object.recursiveIterate
		2. array.difference
		3. testing.assertThrows
		4. testing.assertGreaterThan
		5. testing.assertLessThan
		6. testing.assertForEach
		7. testing.Range

	* TEST all the remaining currently untested functions from prev. releases:
		* array:
			1. insertion
			2. difference
		* number:
			1. difference
		* string:
			1. charCodeAt
		* object:
			1. recursiveIterate
		* functional:
			1. nil
			2. negate
		* testing:
			1. assertForEach
				* test it alongside `Range`
				[note: `Range` exists specifically to mimic two-counter loops,
					like when trying to test a `assert.strictEqual(x.read(i), known[j])` thing,
					and `j` runs from 0 to CountOfItems, and `i` runs from `from` to `from + CountOfItems`
				]

	* To test [new]:
		26. object.descriptor:
			* `Writable()`
				* one with `Writable()`, another without - ensure that writes to one are legal and to another - not
			* `Configurable()`
				1. two descriptors - one without, another with 'Configurable()'
				2. check that the first cannot be re-configured
				3. check that the second can be reconfigured
			* `Get()`
			* `Set()`
			* `Get()` + `Set()` + `Configurable()`:
				* replace a setter in the getter/setter pair on an object by utilizing `Conrigurable()`

	* TODO: fix the publish pipeline
		1. replace the expired NPM_TOKEN secret
		2. update version to 1.0.0 right before publishing
			[already have the workflow]
		3. fix the publish variables (they could be outdated)

	* Update wiki [all the links - new AND old]

	Fix JSDOC:
		1. simplify, improve discriptiveness
		2. modules left:
			* inplace
			* object
			* types
			* functional
